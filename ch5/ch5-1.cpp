#include "book.h"

int area(int length, int width);

int main()
{
	int s1=area(7;
	int s2=area(7)
	Int s3=area(7);
	int s4=area('7);

	// yet more errors!

	int x0=arena(7);
	int x1=area(7);
	int x2=area("seven",2);
	bool x3=area(7,4);
}

int area(int length, int width)
{
	return length*width;
}
